image: golang:1.25.4

stages:
  - build
  - test
  - release

build:
  stage: build
  script:
    - make build
  artifacts:
    paths:
      - mcphe

test:
  stage: test
  script:
    - make test
    # need to install npm and nodejs
    - apt-get update && apt-get install -y curl
    - curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
    - apt-get install -y nodejs
    - cd plugin-devel && npm install
    - cd ..
    - make plugin-tests

release:
  stage: release
  script:
    - make release
  artifacts:
    paths:
      - mcphe
  only:
    - tags